# Phase 5: Active Session

## Goal
The core value screen — start a session from a template, log sets inline, complete workout.

## What you'll learn
- Why this screen uses useState/useReducer + direct SQLite writes (NOT React Hook Form)
- How to snapshot a template into a session
- Inline expansion/collapse UI pattern
- The Planning vs Execution boundary in practice

## Tasks
- [ ] Implement session start (snapshot template into session)
- [ ] Create active session screen (screens/active-session.md)
- [ ] Build collapsed exercise row (name + last set summary)
- [ ] Build expanded exercise row (previous performance, current sets)
- [ ] Implement set logging (reps + weight input, minimal taps)
- [ ] Add warm-up set support (explicit action, visual distinction)
- [ ] Implement exercise reorder (move down 1)
- [ ] Create session completion screen (screens/session-complete.md)
- [ ] Wire up session completion and summary

## Notes
